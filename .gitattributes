# Next.js emits generated chunks whose exact bytes can contain trailing spaces.
# Asset hashes verify them; Git's whitespace checker must not rewrite those bytes.
internal/consoleassets/dist/** -whitespace
