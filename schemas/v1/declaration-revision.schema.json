{
  "$defs": {
    "contract-extension": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "pattern": "^x-[a-z][a-z0-9.-]{0,62}$",
          "type": "string"
        },
        "valueDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        }
      },
      "required": [
        "name",
        "valueDigest"
      ],
      "type": "object"
    },
    "declaration-operation": {
      "additionalProperties": false,
      "properties": {
        "adapterId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "artifactDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "idempotent": {
          "type": "boolean"
        },
        "inputDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "offsiteRunSpec": {
          "anyOf": [
            {
              "$ref": "#/$defs/offsite-run-spec"
            },
            {
              "type": "null"
            }
          ]
        },
        "operationId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "operationType": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "sequence": {
          "minimum": 1,
          "type": "integer"
        },
        "targetId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        }
      },
      "required": [
        "sequence",
        "operationId",
        "operationType",
        "adapterId",
        "targetId",
        "inputDigest",
        "artifactDigest",
        "idempotent"
      ],
      "type": "object"
    },
    "offsite-run-spec": {
      "additionalProperties": false,
      "properties": {
        "g008EvidenceDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "generationId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "maximumBytes": {
          "minimum": 1,
          "type": "integer"
        },
        "maximumLists": {
          "minimum": 1,
          "type": "integer"
        },
        "maximumPuts": {
          "minimum": 1,
          "type": "integer"
        },
        "maximumRetainedGenerations": {
          "minimum": 1,
          "type": "integer"
        },
        "observerReferenceId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "parentReferenceId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "repositoryKeyReferenceId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "repositoryUrl": {
          "maxLength": 4096,
          "minLength": 12,
          "type": "string"
        },
        "retentionSeconds": {
          "minimum": 1,
          "type": "integer"
        },
        "ruleDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "ruleLimit": {
          "minimum": 1,
          "type": "integer"
        },
        "sessionTtlSeconds": {
          "maximum": 900,
          "minimum": 1,
          "type": "integer"
        },
        "snapshotPath": {
          "maxLength": 4096,
          "minLength": 2,
          "pattern": "^/[^\\x00]*$",
          "type": "string"
        },
        "sourcePointId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        }
      },
      "required": [
        "generationId",
        "sourcePointId",
        "snapshotPath",
        "repositoryUrl",
        "parentReferenceId",
        "repositoryKeyReferenceId",
        "observerReferenceId",
        "ruleDigest",
        "g008EvidenceDigest",
        "maximumBytes",
        "maximumPuts",
        "maximumLists",
        "maximumRetainedGenerations",
        "ruleLimit",
        "retentionSeconds",
        "sessionTtlSeconds"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/declaration-revision",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "agentSessionId": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "contentDigest": {
      "pattern": "^sha256:[a-f0-9]{64}$",
      "type": "string"
    },
    "createdAt": {
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
      "type": "string"
    },
    "createdBy": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "declarationId": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "declarationType": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "extensions": {
      "items": {
        "$ref": "#/$defs/contract-extension"
      },
      "maxItems": 64,
      "type": "array"
    },
    "operations": {
      "items": {
        "$ref": "#/$defs/declaration-operation"
      },
      "maxItems": 256,
      "minItems": 1,
      "type": "array"
    },
    "recoveryEpoch": {
      "minimum": 0,
      "type": "integer"
    },
    "revision": {
      "minimum": 1,
      "type": "integer"
    },
    "schema": {
      "enum": [
        "vegastack-labs.dev/declaration-revision"
      ],
      "type": "string"
    },
    "schemaVersion": {
      "enum": [
        "1.0.0"
      ],
      "type": "string"
    },
    "stateRevision": {
      "minimum": 0,
      "type": "integer"
    },
    "status": {
      "enum": [
        "committed",
        "draft",
        "superseded"
      ],
      "type": "string"
    }
  },
  "required": [
    "schema",
    "schemaVersion",
    "declarationId",
    "declarationType",
    "revision",
    "stateRevision",
    "recoveryEpoch",
    "contentDigest",
    "status",
    "operations",
    "createdAt",
    "createdBy",
    "agentSessionId",
    "extensions"
  ],
  "title": "VegaStack Labs declaration revision",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
