{
  "$defs": {
    "contract-extension": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "pattern": "^x-[a-z][a-z0-9.-]{0,62}$",
          "type": "string"
        },
        "valueDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        }
      },
      "required": [
        "name",
        "valueDigest"
      ],
      "type": "object"
    },
    "declaration-operation": {
      "additionalProperties": false,
      "properties": {
        "adapterId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "artifactDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "idempotent": {
          "type": "boolean"
        },
        "inputDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "operationId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "operationType": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "sequence": {
          "minimum": 1,
          "type": "integer"
        },
        "targetId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        }
      },
      "required": [
        "sequence",
        "operationId",
        "operationType",
        "adapterId",
        "targetId",
        "inputDigest",
        "artifactDigest",
        "idempotent"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/declaration-revision-request",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "declarationId": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "declarationType": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "expectedRevision": {
      "minimum": 1,
      "type": "integer"
    },
    "expectedStateRevision": {
      "minimum": 0,
      "type": "integer"
    },
    "extensions": {
      "items": {
        "$ref": "#/$defs/contract-extension"
      },
      "maxItems": 64,
      "type": "array"
    },
    "operations": {
      "items": {
        "$ref": "#/$defs/declaration-operation"
      },
      "maxItems": 256,
      "minItems": 1,
      "type": "array"
    },
    "reasonDigest": {
      "pattern": "^sha256:[a-f0-9]{64}$",
      "type": "string"
    },
    "recoveryEpoch": {
      "minimum": 0,
      "type": "integer"
    },
    "schema": {
      "enum": [
        "vegastack-labs.dev/declaration-revision-request"
      ],
      "type": "string"
    },
    "schemaVersion": {
      "enum": [
        "1.0.0"
      ],
      "type": "string"
    }
  },
  "required": [
    "schema",
    "schemaVersion",
    "declarationId",
    "declarationType",
    "expectedRevision",
    "expectedStateRevision",
    "recoveryEpoch",
    "operations",
    "reasonDigest",
    "extensions"
  ],
  "title": "VegaStack Labs declaration revision request",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
