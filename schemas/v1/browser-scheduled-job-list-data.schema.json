{
  "$defs": {
    "browser-scheduled-job": {
      "additionalProperties": false,
      "properties": {
        "jobId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "policyId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "policyRevision": {
          "minimum": 1,
          "type": "integer"
        },
        "reasonCode": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "recoveryEpoch": {
          "minimum": 0,
          "type": "integer"
        },
        "scheduledAt": {
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
          "type": "string"
        },
        "schema": {
          "enum": [
            "vegastack-labs.dev/browser-scheduled-job"
          ],
          "type": "string"
        },
        "schemaVersion": {
          "enum": [
            "1.0.0"
          ],
          "type": "string"
        },
        "stateRevision": {
          "minimum": 0,
          "type": "integer"
        },
        "status": {
          "enum": [
            "queued",
            "blocked",
            "skipped",
            "running",
            "retry-wait",
            "succeeded",
            "cancelled",
            "failed",
            "uncertain"
          ],
          "type": "string"
        },
        "windowClosesAt": {
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
          "type": "string"
        }
      },
      "required": [
        "schema",
        "schemaVersion",
        "jobId",
        "policyId",
        "policyRevision",
        "status",
        "reasonCode",
        "scheduledAt",
        "windowClosesAt",
        "stateRevision",
        "recoveryEpoch"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/browser-scheduled-job-list-data",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "items": {
      "items": {
        "$ref": "#/$defs/browser-scheduled-job"
      },
      "maxItems": 100,
      "type": "array"
    },
    "nextCursor": {
      "maxLength": 2048,
      "type": [
        "string",
        "null"
      ]
    },
    "recoveryEpoch": {
      "minimum": 0,
      "type": "integer"
    },
    "schema": {
      "enum": [
        "vegastack-labs.dev/browser-scheduled-job-list-data"
      ],
      "type": "string"
    },
    "schemaVersion": {
      "enum": [
        "1.0.0"
      ],
      "type": "string"
    },
    "stateRevision": {
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "schema",
    "schemaVersion",
    "items",
    "nextCursor",
    "stateRevision",
    "recoveryEpoch"
  ],
  "title": "VegaStack Labs browser scheduled job list data",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
