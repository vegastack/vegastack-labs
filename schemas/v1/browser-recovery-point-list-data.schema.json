{
  "$defs": {
    "browser-recovery-point": {
      "additionalProperties": false,
      "allOf": [
        {
          "if": {
            "properties": {
              "sourceKind": {
                "const": "fixture"
              }
            },
            "required": [
              "sourceKind"
            ]
          },
          "then": {
            "properties": {
              "proofClass": {
                "const": "fixture"
              }
            }
          }
        },
        {
          "if": {
            "properties": {
              "verificationStatus": {
                "const": "verified"
              }
            },
            "required": [
              "verificationStatus"
            ]
          },
          "then": {
            "properties": {
              "verifiedAt": {
                "type": "string"
              }
            }
          }
        }
      ],
      "properties": {
        "contentDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "createdAt": {
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
          "type": "string"
        },
        "manifestDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "pointId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "proofClass": {
          "enum": [
            "fixture",
            "live"
          ],
          "type": "string"
        },
        "reasonCode": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "recoveryEpoch": {
          "minimum": 0,
          "type": "integer"
        },
        "schema": {
          "enum": [
            "vegastack-labs.dev/browser-recovery-point"
          ],
          "type": "string"
        },
        "schemaVersion": {
          "enum": [
            "1.0.0"
          ],
          "type": "string"
        },
        "sourceKind": {
          "enum": [
            "fixture",
            "local",
            "independent"
          ],
          "type": "string"
        },
        "verificationStatus": {
          "enum": [
            "pending",
            "verified",
            "failed"
          ],
          "type": "string"
        },
        "verifiedAt": {
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "schema",
        "schemaVersion",
        "pointId",
        "sourceKind",
        "proofClass",
        "contentDigest",
        "manifestDigest",
        "createdAt",
        "verifiedAt",
        "verificationStatus",
        "reasonCode",
        "recoveryEpoch"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/browser-recovery-point-list-data",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "items": {
      "items": {
        "$ref": "#/$defs/browser-recovery-point"
      },
      "maxItems": 100,
      "type": "array"
    },
    "nextCursor": {
      "maxLength": 2048,
      "type": [
        "string",
        "null"
      ]
    },
    "recoveryEpoch": {
      "minimum": 0,
      "type": "integer"
    },
    "schema": {
      "enum": [
        "vegastack-labs.dev/browser-recovery-point-list-data"
      ],
      "type": "string"
    },
    "schemaVersion": {
      "enum": [
        "1.0.0"
      ],
      "type": "string"
    },
    "stateRevision": {
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "schema",
    "schemaVersion",
    "items",
    "nextCursor",
    "stateRevision",
    "recoveryEpoch"
  ],
  "title": "VegaStack Labs browser recovery point list data",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
