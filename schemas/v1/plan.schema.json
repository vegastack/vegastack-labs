{
  "$defs": {
    "contract-extension": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "pattern": "^x-[a-z][a-z0-9.-]{0,62}$",
          "type": "string"
        },
        "valueDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        }
      },
      "required": [
        "name",
        "valueDigest"
      ],
      "type": "object"
    },
    "plan-binding": {
      "additionalProperties": false,
      "properties": {
        "contractVersion": {
          "pattern": "^1\\.[0-9]+\\.[0-9]+$",
          "type": "string"
        },
        "declarationRevision": {
          "minimum": 1,
          "type": "integer"
        },
        "observationFingerprint": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "policyVersion": {
          "pattern": "^1\\.[0-9]+\\.[0-9]+$",
          "type": "string"
        },
        "priorStateRevision": {
          "minimum": 0,
          "type": "integer"
        },
        "reasonDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "recoveryEpoch": {
          "minimum": 0,
          "type": "integer"
        },
        "stateRevision": {
          "minimum": 1,
          "type": "integer"
        },
        "targetDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "toolVersion": {
          "pattern": "^1\\.[0-9]+\\.[0-9]+$",
          "type": "string"
        }
      },
      "required": [
        "recoveryEpoch",
        "priorStateRevision",
        "stateRevision",
        "declarationRevision",
        "observationFingerprint",
        "targetDigest",
        "reasonDigest",
        "policyVersion",
        "toolVersion",
        "contractVersion"
      ],
      "type": "object"
    },
    "plan-operation": {
      "additionalProperties": false,
      "properties": {
        "adapterId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "artifactDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "executorId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "idempotent": {
          "type": "boolean"
        },
        "inputDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "operationId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "operationType": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "sequence": {
          "minimum": 1,
          "type": "integer"
        },
        "targetId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        }
      },
      "required": [
        "sequence",
        "operationId",
        "operationType",
        "adapterId",
        "executorId",
        "targetId",
        "inputDigest",
        "artifactDigest",
        "idempotent"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/plan",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "authorizationBranch": {
      "enum": [
        "human",
        "preauthorized"
      ],
      "type": "string"
    },
    "binding": {
      "$ref": "#/$defs/plan-binding"
    },
    "createdAt": {
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
      "type": "string"
    },
    "declarationId": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "executorId": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": [
        "string",
        "null"
      ]
    },
    "executorMode": {
      "enum": [
        "central",
        "external"
      ],
      "type": "string"
    },
    "expiresAt": {
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
      "type": "string"
    },
    "extensions": {
      "items": {
        "$ref": "#/$defs/contract-extension"
      },
      "maxItems": 64,
      "type": "array"
    },
    "operations": {
      "items": {
        "$ref": "#/$defs/plan-operation"
      },
      "maxItems": 256,
      "minItems": 1,
      "type": "array"
    },
    "planDigest": {
      "pattern": "^sha256:[a-f0-9]{64}$",
      "type": "string"
    },
    "planId": {
      "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
      "type": "string"
    },
    "readableDigest": {
      "pattern": "^sha256:[a-f0-9]{64}$",
      "type": "string"
    },
    "risk": {
      "enum": [
        "control-plane",
        "destructive",
        "infrastructure",
        "production-like",
        "routine"
      ],
      "type": "string"
    },
    "schema": {
      "enum": [
        "vegastack-labs.dev/plan"
      ],
      "type": "string"
    },
    "schemaVersion": {
      "pattern": "^1\\.[0-9]+\\.[0-9]+$",
      "type": "string"
    },
    "status": {
      "enum": [
        "approved",
        "awaiting-acknowledgement",
        "cancelled",
        "expired",
        "planned"
      ],
      "type": "string"
    }
  },
  "required": [
    "schema",
    "schemaVersion",
    "planId",
    "planDigest",
    "declarationId",
    "binding",
    "operations",
    "status",
    "risk",
    "authorizationBranch",
    "executorMode",
    "executorId",
    "createdAt",
    "expiresAt",
    "readableDigest",
    "extensions"
  ],
  "title": "VegaStack Labs plan",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
