{
  "$defs": {
    "inventory-draft-ref": {
      "additionalProperties": false,
      "properties": {
        "draftId": {
          "maxLength": 128,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._+:-]{0,127}$",
          "type": "string"
        },
        "draftRevision": {
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [
        "draftId",
        "draftRevision"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/inventory-export-data",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "algorithm": {
      "enum": [
        "ed25519"
      ],
      "type": "string"
    },
    "contentDigest": {
      "pattern": "^sha256:[0-9a-f]{64}$",
      "type": "string"
    },
    "draft": {
      "$ref": "#/$defs/inventory-draft-ref"
    },
    "exportId": {
      "pattern": "^sha256:[0-9a-f]{64}$",
      "type": "string"
    },
    "keyFingerprint": {
      "pattern": "^sha256:[0-9a-f]{64}$",
      "type": "string"
    },
    "keyId": {
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._+:-]{0,127}$",
      "type": "string"
    },
    "publicationStatus": {
      "enum": [
        "published"
      ],
      "type": "string"
    },
    "recoveryEpoch": {
      "minimum": 0,
      "type": "integer"
    },
    "signedBytesBase64": {
      "maxLength": 25165824,
      "pattern": "^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$",
      "type": "string"
    },
    "stateRevision": {
      "minimum": 0,
      "type": "integer"
    },
    "subjectKind": {
      "enum": [
        "draft"
      ],
      "type": "string"
    },
    "verificationStatus": {
      "enum": [
        "verified"
      ],
      "type": "string"
    }
  },
  "required": [
    "exportId",
    "subjectKind",
    "draft",
    "stateRevision",
    "recoveryEpoch",
    "contentDigest",
    "algorithm",
    "keyId",
    "keyFingerprint",
    "verificationStatus",
    "publicationStatus",
    "signedBytesBase64"
  ],
  "title": "VegaStack Labs inventory export data",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
