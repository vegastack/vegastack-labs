{
  "$defs": {
    "browser-run": {
      "additionalProperties": false,
      "properties": {
        "cancellationRequested": {
          "type": "boolean"
        },
        "changed": {
          "type": "boolean"
        },
        "createdAt": {
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
          "type": "string"
        },
        "extensions": {
          "items": {
            "$ref": "#/$defs/contract-extension"
          },
          "maxItems": 64,
          "type": "array"
        },
        "planDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        },
        "planId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "recoveryEpoch": {
          "minimum": 0,
          "type": "integer"
        },
        "rollbackStatus": {
          "enum": [
            "not-requested",
            "required",
            "separate-plan"
          ],
          "type": "string"
        },
        "runId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "schema": {
          "enum": [
            "vegastack-labs.dev/browser-run"
          ],
          "type": "string"
        },
        "schemaVersion": {
          "enum": [
            "1.0.0"
          ],
          "type": "string"
        },
        "stateRevision": {
          "minimum": 0,
          "type": "integer"
        },
        "status": {
          "enum": [
            "cancelled",
            "failed",
            "interrupted",
            "partial",
            "queued",
            "running",
            "succeeded"
          ],
          "type": "string"
        },
        "steps": {
          "items": {
            "$ref": "#/$defs/browser-run-step"
          },
          "maxItems": 256,
          "type": "array"
        },
        "updatedAt": {
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$",
          "type": "string"
        },
        "verificationDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": [
            "string",
            "null"
          ]
        },
        "verificationStatus": {
          "enum": [
            "failed",
            "incomplete",
            "pending",
            "verified"
          ],
          "type": "string"
        }
      },
      "required": [
        "schema",
        "schemaVersion",
        "runId",
        "planId",
        "planDigest",
        "status",
        "steps",
        "cancellationRequested",
        "rollbackStatus",
        "verificationStatus",
        "verificationDigest",
        "changed",
        "stateRevision",
        "recoveryEpoch",
        "createdAt",
        "updatedAt",
        "extensions"
      ],
      "type": "object"
    },
    "browser-run-step": {
      "additionalProperties": false,
      "properties": {
        "operationId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "operationType": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "sequence": {
          "minimum": 1,
          "type": "integer"
        },
        "status": {
          "enum": [
            "cancelled",
            "failed",
            "interrupted",
            "partial",
            "queued",
            "running",
            "succeeded"
          ],
          "type": "string"
        },
        "stepId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "targetId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        }
      },
      "required": [
        "sequence",
        "operationId",
        "operationType",
        "targetId",
        "stepId",
        "status"
      ],
      "type": "object"
    },
    "contract-extension": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "pattern": "^x-[a-z][a-z0-9.-]{0,62}$",
          "type": "string"
        },
        "valueDigest": {
          "pattern": "^sha256:[a-f0-9]{64}$",
          "type": "string"
        }
      },
      "required": [
        "name",
        "valueDigest"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/run-presentation",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "completedWork": {
      "items": {
        "$ref": "#/$defs/browser-run-step"
      },
      "maxItems": 256,
      "type": "array"
    },
    "incompleteWork": {
      "items": {
        "$ref": "#/$defs/browser-run-step"
      },
      "maxItems": 256,
      "type": "array"
    },
    "nextSafeAction": {
      "enum": [
        "none; execution completed",
        "inspect before creating another plan",
        "inspect, then resume or cancel through the server",
        "inspect or cancel through the server",
        "recovery required; inspect the durable run",
        "inspect the durable run"
      ],
      "type": "string"
    },
    "run": {
      "$ref": "#/$defs/browser-run"
    }
  },
  "required": [
    "run",
    "completedWork",
    "incompleteWork",
    "nextSafeAction"
  ],
  "title": "VegaStack Labs run presentation",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
