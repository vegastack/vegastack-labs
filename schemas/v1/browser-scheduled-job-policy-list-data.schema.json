{
  "$defs": {
    "browser-scheduled-job-policy": {
      "additionalProperties": false,
      "properties": {
        "actionKind": {
          "enum": [
            "gate-check",
            "observation-refresh",
            "backup-create",
            "backup-integrity-verify",
            "audit-checkpoint-export"
          ],
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        },
        "policyId": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "reasonCode": {
          "pattern": "^[a-z][a-z0-9._:-]{0,127}$",
          "type": "string"
        },
        "recoveryEpoch": {
          "minimum": 0,
          "type": "integer"
        },
        "revision": {
          "minimum": 1,
          "type": "integer"
        },
        "schema": {
          "enum": [
            "vegastack-labs.dev/browser-scheduled-job-policy"
          ],
          "type": "string"
        },
        "schemaVersion": {
          "enum": [
            "1.0.0"
          ],
          "type": "string"
        },
        "stateRevision": {
          "minimum": 0,
          "type": "integer"
        },
        "status": {
          "enum": [
            "active",
            "disabled"
          ],
          "type": "string"
        }
      },
      "required": [
        "schema",
        "schemaVersion",
        "policyId",
        "revision",
        "actionKind",
        "enabled",
        "status",
        "reasonCode",
        "stateRevision",
        "recoveryEpoch"
      ],
      "type": "object"
    }
  },
  "$id": "vegastack-labs.dev/browser-scheduled-job-policy-list-data",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "items": {
      "items": {
        "$ref": "#/$defs/browser-scheduled-job-policy"
      },
      "maxItems": 100,
      "type": "array"
    },
    "nextCursor": {
      "maxLength": 2048,
      "type": [
        "string",
        "null"
      ]
    },
    "recoveryEpoch": {
      "minimum": 0,
      "type": "integer"
    },
    "schema": {
      "enum": [
        "vegastack-labs.dev/browser-scheduled-job-policy-list-data"
      ],
      "type": "string"
    },
    "schemaVersion": {
      "enum": [
        "1.0.0"
      ],
      "type": "string"
    },
    "stateRevision": {
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "schema",
    "schemaVersion",
    "items",
    "nextCursor",
    "stateRevision",
    "recoveryEpoch"
  ],
  "title": "VegaStack Labs browser scheduled job policy list data",
  "type": "object",
  "x-generated-by": "go run ./tooling/generate-contracts --write; DO NOT EDIT"
}
