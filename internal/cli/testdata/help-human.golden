vsk-labs commands

Available commands:
  apply                    Execute one exact current and authorized immutable plan.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --plan-id <id>         Select the exact immutable plan; this does not acknowledge it.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  audit checkpoints        List sanitized audit checkpoints.
    --config <path>        Read one protected server profile.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  audit verify             Verify local audit history against independent checkpoint state.
    --config <path>        Read one protected server profile.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup offsite-retirement dry-run Derive one exact off-site retirement intent from the qualified complete bucket catalog without staging it.
    --config <path>        Read one protected local server profile.
    --file <path>          Read one exact typed backup-offsite-retirement-dry-run-request JSON file (64 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup offsite-retirement stage Derive and stage one exact inert off-site retirement from a qualified complete bucket catalog.
    --config <path>        Read one protected local server profile.
    --file <path>          Read one exact typed backup-offsite-retirement-stage-request JSON file (64 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup policy draft      Validate and store an inert canonical backup-policy draft; application still needs an exact human-approved plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one exact typed backup-policy-draft-request JSON file (64 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup retention-locks draft Store one complete inert local retention-lock catalog and its exact human-plan declaration.
    --config <path>        Read one protected server profile.
    --file <path>          Read one exact typed backup-retention-lock-draft-request JSON file (64 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup retirement draft  Derive and store one exact inert local retirement selection with its credential binding.
    --config <path>        Read one protected local server profile.
    --file <path>          Read one exact typed backup-retirement-draft-request JSON file (64 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup run               Execute one exact approved backup-creation plan; the point remains pending.
    --config <path>        Read one protected server profile.
    --file <path>          Read one exact typed backup-run-request JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup status            Inspect local backup jobs and qualification status.
    --config <path>        Read one protected server profile.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  backup verify            Execute one exact approved local-backup verification plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one exact typed backup-verify-request JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  credential activate      Create an inert credential lifecycle draft; execution requires a separate exact human-approved plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one material-free lifecycle-request JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  credential import        Import a local encrypted credential as an inert draft.
    --config <path>        Read one protected local server profile.
    --consumer-id <id>     Bind the draft to one consumer.
    --expected-state-revision <revision> Require one current state revision.
    --idempotency-key <id> Bind retries to one import intent.
    --input-fd <descriptor> Read private bytes from an already-open descriptor instead of stdin.
    --material-version <id> Name the proposed material version.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --purpose-id <id>      Bind the draft to one purpose.
    --recovery-epoch <epoch> Require one current recovery epoch.
    --reference-id <id>    Bind the draft to one credential reference.
    --resolver-id <id>     Select the native systemd resolver. Allowed: native-systemd.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
    --target-id <id>       Bind the draft to one public target.
  credential recover       Create an inert credential lifecycle draft; execution requires a separate exact human-approved plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one material-free lifecycle-request JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  credential revoke        Create an inert credential lifecycle draft; execution requires a separate exact human-approved plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one material-free lifecycle-request JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  credential rotate        Create an inert credential lifecycle draft; execution requires a separate exact human-approved plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one material-free lifecycle-request JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  credential stage         Create an inert credential lifecycle draft; execution requires a separate exact human-approved plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one material-free lifecycle-request JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  database status          Inspect control-database status.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  gate check               Check one gate for one subject without changing state.
    --config <path>        Read one protected server profile.
    --gate-id <id>         Select one generated gate ID.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
    --subject-id <id>      Select one subject ID.
  gate evidence            Submit a bounded typed proof as an inert change; no gate pass is set.
    --config <path>        Read one protected server profile.
    --file <path>          Read one exact typed evidence-request JSON file (64 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  gate inspect             Inspect one scoped gate and derived blocker.
    --config <path>        Read one protected server profile.
    --gate-id <id>         Select one generated gate ID.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  gate list                List scoped generated gate definitions and derived blockers.
    --config <path>        Read one protected server profile.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  gate profile draft       Submit a bounded profile/policy candidate as an inert change; application still needs an exact human-approved plan.
    --config <path>        Read one protected server profile.
    --file <path>          Read one exact typed profile-draft JSON file (4 KiB max).
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  help                     Show generated command help.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  inventory diff           Compare one inert draft or local candidate with a compatible inert draft.
    --captured-at <timestamp> Record an RFC 3339 UTC capture time.
    --config <path>        Read the protected server profile at this explicit path.
    --draft-id <id>        Select an existing inert draft.
    --draft-revision <revision> Select the exact inert draft revision.
    --file <path>          Read one protected local candidate file.
    --format <format>      Select the explicit candidate format. Allowed: labs-sheet1-csv, typed-json.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
    --source-revision <revision> Record the source revision supplied by its owner.
  inventory export         Publish one verified signed inert-draft export.
    --config <path>        Read the protected server profile at this explicit path.
    --draft-id <id>        Select an existing inert draft.
    --draft-revision <revision> Select the exact inert draft revision.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  inventory import         Validate and store one inventory candidate as an inert draft.
    --captured-at <timestamp> Record an RFC 3339 UTC capture time.
    --config <path>        Read the protected server profile at this explicit path.
    --expected-state-revision <revision> Require this current state revision.
    --file <path>          Read one protected local candidate file.
    --format <format>      Select the explicit candidate format. Allowed: labs-sheet1-csv, typed-json.
    --idempotency-key <key> Supply one opaque retry key.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
    --source-revision <revision> Record the source revision supplied by its owner.
  plan                     Create an immutable plan from one exact inert declaration revision.
    --config <path>        Read the protected server profile at this explicit path.
    --declaration-id <id>  Select one exact inert declaration.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --revision <revision>  Select the exact positive declaration revision.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  recovery witness collect Collect one bounded independent recovery witness on a separately administered custodian.
    --file <path>          Read one exact public recovery binding and required-boundary document (64 KiB max).
    --material-fd <fd>     Read the independently held protected material from an inherited descriptor.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
    --signing-key-fd <fd>  Read the protected witness signing seed from an inherited descriptor.
  release inspect          Inspect a local release manifest and compatibility without claiming cryptographic verification.
    --manifest <path>      Read the local release manifest at this path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  release verify           Verify a signed local manifest and explicitly selected assets against a supplied offline policy.
    --all                  Explicitly verify every asset in the manifest.
    --asset <id>           Verify one named asset; repeat for additional assets.
    --manifest <path>      Read the local release manifest at this path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --policy <path>        Read the supplied local trust policy at this path.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  run cancel               Request server-owned cancellation of one durable run at a safe boundary.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --run-id <id>          Select the exact durable run.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  run inspect              Inspect one durable run without retrying apply.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --run-id <id>          Select the exact durable run.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  run resume               Request server-owned resumption of one safely resumable durable run.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --run-id <id>          Select the exact durable run.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  server api-ssh           Serve one constrained SSH API frame through the persistent control service.
    --config <path>        Read the protected server profile at this explicit path.
    --device-id <id>       Bind the server-configured forced command to this verified device.
    --ssh-principal-id <id> Bind the server-configured forced command to this verified SSH principal.
  server run               Run the persistent control service in the foreground.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  server status            Query control-service health.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  status                   Show the current platform summary.
    --config <path>        Read the protected server profile at this explicit path.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.
  version                  Show the vsk-labs build and contract version.
    --output <format>      Select human or versioned JSON output. Allowed: human, json.
    --schema-version <major> Select the machine-contract schema major. Allowed: 1.

Planned commands (unavailable):
  audit                    Inspect sanitized audit history.
  connect                  Open a constrained connection to an authorized node endpoint.
  control-plane plan       Create an immutable control-plane change plan.
  control-plane recover    Create an inert control-plane recovery change.
  control-plane verify     Verify control-plane health and authority.
  database backup          Create a verified control-database backup.
  database export          Export authorized sanitized control data.
  database restore         Create an inert control-database restore change.
  database verify          Verify control-database integrity or backup content.
  device approve           Create an inert device-approval change.
  device request           Create an inert device-enrollment request.
  device revoke            Create an inert device-revocation change.
  doctor                   Diagnose one actionable platform invariant at a time.
  maintenance plan         Create an immutable maintenance plan.
  maintenance run          Run one authorized maintenance plan.
  node add                 Create an inert managed-node change.
  node discover            Discover a candidate managed node.
  node inspect             Inspect a managed node.
  node nominate            Create an inert role-nomination change.
  node quarantine          Create an inert node-quarantine change.
  node replace             Create an inert node-replacement change.
  restore plan             Create an immutable restore plan.
  restore run              Run one authorized restore plan.
  restore verify           Verify a completed restore.
  service deploy           Create an inert service-deployment change.
  service plan             Create an inert service change and request its plan.
  service rollback         Create an inert service-rollback change.
  user offboard            Create an inert user-offboarding change.
  user onboard             Create an inert user-onboarding change.
  user resume              Create an inert user-resumption change.
  user suspend             Create an inert user-suspension change.
