# Full-scope development roadmap

Status: phase boundaries and complete scope allocation approved by the user on 26-08-2026. The user has adopted the [operating mandate](operating-mandate.md), accepted Phase 0 on 07-09-2026, and directed that the entire documented scope be delivered phase by phase, with multiple issues per phase. [Phase 1](phases/01-portable-executable-and-generated-contracts.md) is active; later phase solutions, issue batches, and operational authority retain their separate approvals.

Rollout timing confirmed on 26-08-2026: finish and verify the complete v1 platform before the first lab onboarding rehearsal. Isolated implementation tests still run throughout development; the inventory fleet is not an early deployment pilot. [D-117](../decisions-and-sources.md#d-117)

The same user direction requires basic, reliable hardening whenever a machine is onboarded or added to the lab, with Ansible performing the required host security and role configuration. This is assigned within the existing phases below, without renumbering. The [onboarding requirement and selected tool/profile contract](../host-onboarding-and-hardening.md) distinguish confirmed outcomes and selected tools from exact profile settings still awaiting phase/batch review.

## Scope and finish line

Cover the accepted v1 portable platform, selected adapters, VegaStack Labs profile, operator experience, safety, verification, distribution, and runbooks. Do not reduce the project to a CLI prototype or omit operational acceptance. The existing [specification map](../../README.md#documentation-map), decision register, support matrix, and deployment gates remain authoritative.

There are two distinct completion claims:

- **Development complete:** the specified software, documentation, packaging, and required platform/integration tests pass. Fixtures are identified as fixtures; required real-platform evidence cannot be replaced by cross-compilation.
- **VegaStack Labs accepted:** the existing deployment phases have their real site/provider evidence, authorized operations, and recovery demonstrations. Software completion alone does not satisfy this claim.

Existing exclusions remain excluded: Chaabi Prod migration, general application-by-application placement, Kubernetes/Swarm, distributed storage or automatic stateful failover, Workspace account/licence provisioning, a generic plugin framework, and a custom privileged managed-node agent. Optional mechanisms in the GitHub dependency matrix or D1 discussion do not automatically become mandatory features or enabled integrations. Their enablement must be explicit; disabled optional integrations must not disable local core operation.

## Planning and sequencing

Plan the whole scope here, then detail one phase at a time. The approved order follows the foundations needed by later phases. Before each batch, replace broad dependencies with exact issue links and confirm the actual interfaces and verification inputs. Independent work is parallel only when the batch explicitly permits it.

Each phase gets `phases/00-<name>.md`, `phases/01-<name>.md`, and so on, containing requirements, solution, exclusions, execution defaults, issue index, acceptance owner, and approvals. Issues use `0.1`, `0.2`, `1.1`, etc. The issue families below are decomposition guidance, not published issues or frozen implementation steps.

Security, tests, documentation, and recovery accompany the capability that needs them. The final acceptance phase integrates and rechecks those obligations; it is not where safety work begins. Later capabilities extend their CLI/API/Console surfaces in their own issues.

Host hardening is an admission requirement, not a final security cleanup: phase 0 settles the shared baseline and its compatibility boundaries; phases 1 and 5 supply its generated contracts and evidence enforcement; phase 6 implements and verifies each supported OS/role profile before workload admission; phases 7–9 recheck it as access and workloads are added; phase 10 supplies recurring drift checks and repair workflows; phase 11 proves the integrated supported-platform matrix. Unknown or untested platforms cannot be treated as hardened merely because a generic script completed.

If a later phase supplies a required safety dependency, the public command remains blocked until that dependency exists. Synthetic adapters, identities, evidence and release artifacts are isolated test inputs, never a shipping bypass switch. Earlier phases may prove a specifically bounded contract without claiming the complete live workflow.

## Development phases

| Phase | Outcome and scope | Likely issue families | Phase completion evidence |
|---|---|---|---|
| **0 — Development foundation and specification reconciliation** | A new agent can discover the mandate, obtain a clean checkout, run the available checks, and complete a small approved issue through the permitted review/merge route. Resolve or assign specification conflicts before they affect executable work. | Development authority and entry-point integration; historical audit/generation ownership; link/contract validation; bootstrap/profile/transport/Slack-acknowledgement contract; shared host-hardening baseline, compatibility and admission contract; Go/web/test repository scaffold and MIT licensing; development CI and merge conventions; a real workflow pilot. | Reproducible clean-checkout checks, generated-drift policy, a completed pilot with review/evidence, approved bootstrap/hardening contracts with downstream owners, and an explicit resolution/owner for every known planning conflict. Include generic OSS/profile separation, bounded local setup, account-free read/preparation and recovery, and failed-closed Slack-required bootstrap/apply fixtures. No claim that the platform or fleet is operational. |
| **1 — Portable executable and generated contracts** | One `vsk-labs` executable exposes a consistent generated command/schema/error contract, portable client foundations, and release-verification primitives. Unimplemented capabilities fail explicitly rather than pretending to work. | Command metadata graph; public declaration/result schemas; JSON normalization and error/exit behavior; generated help/API-client foundations; OS paths, credentials and direct-argument transport; release manifest/bundle verification. | Cross-platform contract goldens, malformed/unsupported input denials, stdout/stderr and noninteractive behavior, generation drift checks, and tampered/incompatible release fixtures. No support claim from buildability alone. |
| **2 — Authoritative control service and inventory** | `vsk-labs server run` owns SQLite and the versioned API; clients can inspect/export inventory and validate inert import drafts without direct database access. | Service lifecycle and protected local transport; schema/migrations/revisions and local online-backup/rollback primitives; draft declarations and reference validation; inventory/aliases/observations; API filtering/pagination and compatibility; local identity binding; health and safe mode. | Clean initialization/migration and restoration of the pre-migration copy, conflicts and invalid references, declared-versus-observed behavior, access/ownership denial, disk/integrity failure handling, and CLI/API read parity. Imports remain drafts until the phase-4 plan transition; infrastructure mutation is unavailable. |
| **3 — Secure read Console and operator access** | The embedded VegaStack Design System Console presents the implemented core state through the generated client, with secure browser/session handling and truthful unavailable/stale states. | Static web embedding and VDS setup; authentication/session/revocation interfaces; read screens/navigation; accessibility and responsive layouts; browser security controls; provider-read adapter boundaries and fixtures. | Authenticated reads, unauthorized/expired-session and origin/CSRF/CSP tests, required browser/accessibility checks, and partial-outage/staleness fixtures. Missing providers are visible, not simulated as healthy. Profile-specific live Access ingress remains gated. |
| **4 — Declarations, plans, authorization, and execution** | CLI and Console create the same inert declaration revision and immutable plan; only an authorized exact plan can execute through the shared engine. | Draft/change constructors and validation; canonical plan identity; desired versus effective role/risk/acknowledgement policy; fixed operational policies and exact low-risk deployment class; run/lease/state machines; external-executor claims; progress/cancellation/resume; typed adapter ownership/import/detach and safe uninstall. | CLI/browser digest parity; changed intent remaining inert; stale/expired/unauthorized plan denial; epoch/lease/conflict tests; interrupted/partial/recovery outcomes; deterministic apply/verify using disposable or fake adapters. Production-like paths always require their maintainer. |
| **5 — Evidence, secrets, backups, and recovery** | The platform can derive gate readiness, handle secret references safely, create verified backups, preserve audit evidence, and recover one authoritative control history. | Generated gate/evidence evaluators; secret-reference, native local credential resolver and optional 1Password adapter contracts/rotation; SQLite online backups and signed exports; local backup path, restic/R2 compatibility qualification and retention separation; audit chain/checkpoints; recovery epochs, fencing and restore; exact scheduled-job policies. | Invalid/expired/replaced evidence denial; secret redaction and permission tests; consistent backups and functional isolated restores; missing/corrupt object, key, capacity and lock failures; old-writer/old-plan denial; recovery-mode safety. Live vault custody and storage grants remain external evidence. |
| **6 — Host lifecycle and control-plane bootstrap** | Deterministic SSH/Ansible workflows discover, qualify, harden, configure, quarantine, replace, and recover supported hosts; local control-plane setup establishes one authoritative service before independently scoped operator/node enrollment. No host receives its managed workload role before its applicable baseline passes. | Typed physical/qualified-virtual identity and hardware/inventory qualification; versioned Debian, Ubuntu and macOS hardening profiles; common and role-specific account/SSH/firewall/OS controls; admission evidence; node roles and aliases; finite local installation manifest and server-owned setup/audit path; service installation/rebuild; Mac accounts/services/developer conveniences; power/battery/thermal controls and manual network procedures. | Real supported-OS role tests on approved disposable targets; unchanged second-apply configuration; exact host identity/limit and lockout prevention; post-reboot positive/negative probes; partial-failure recovery; clean bootstrap/rebuild; cross-user denial; role/resource constraints; unsupported-platform and incomplete-evidence rejection; manual fallback parity. Real hardware burn-in and site role admission remain deployment gates. Physical media disposition is excluded. |
| **7 — People, devices, identity, and network access** | People/device lifecycle and private/public access follow declared grants and flows without creating alternate authorization paths. | Local/SSH operator authentication and optional Workspace subject binding without account provisioning; onboard/suspend/offboard and session revocation; SSH/device grants; Cloudflare Mesh and LAN path selection; managed-network marker; Access/Tunnel adapters and ownership; mobile/operator guidance; connector failure and credential rotation. | Allowed and denied people/device/flow tests, revocation before further mutation, wrong-host/identity denial, provider-outage behavior, safe LAN/manual fallback, and connector/origin isolation fixtures. Actual Mesh pilot and Tunnel qualification are not claimed from mocks. |
| **8 — Hosting, registry, and application lifecycle** | Declared services can be placed and operated through Coolify and Harbor with explicit ownership, signed digest identity, health, data, and recovery contracts. | Coolify control/server/team/resource adapter; service/environment declarations; central/manual digest deployment path; Harbor lifecycle and robots; consistent registry backup/restore/migration procedure; health and safe rollback; corresponding Console views/actions. | Scope/token denials, owned-resource import and idempotence, digest/provenance validation, health/failure/rollback tests, and isolated registry/hosting recovery. Actual Harbor source data and per-application placement are not invented; the CI-executor integration is completed in phase 9. |
| **9 — SCM, managed CI, and application delivery** | Trusted source/artifact workflows can build, attest, publish, and perform an exact authorized application deployment; untrusted code never reaches home runners or deployment credentials. | Replaceable SCM/CI adapters; repository/workflow enrollment; one-job runner controller within the server; rootless Linux jobs and Mac/fallback lane; external log preservation; environment-secret delivery; protected CI-to-Coolify executor; selected optional discovery/event/status capabilities. | Actual assigned-job admission under shared-label races, hostile-job and cross-job isolation, one-job cleanup/cancellation, retained logs, denied untrusted events and stale/unclaimed leases, credential-scope tests, signed artifact-to-running-digest verification, and provider-outage fallback. Repository development CI from phase 0 is distinct from this managed-fleet CI product. |
| **10 — Operations, monitoring, and agent/manual workflows** | Operators can detect failures, maintain the fleet, recover incidents, and use focused agent skills or equivalent manual procedures through the same contract. | Beszel/read monitoring; host-hardening drift checks and planned repair; local outbox and sanitized GitHub alerts; independent Worker/R2 heartbeat and secondary delivery; optional sanitized status projection; maintenance/canary/drain workflows; incident/break-glass reconciliation; canonical Codex/Claude/Hermes skills and trust tests; full runbook coverage. | Induced alert/recovery/dedup and site-silence tests; hardening drift/staleness detection and safe readmission; stale provider states; maintenance failure/recovery; denied direct-provider/secret/agent self-approval paths; instruction/skill generation parity; automated/manual outcome parity. Missing notification providers cannot become core control dependencies. |
| **11 — Distribution and complete v1 acceptance** | The entire accepted v1 can be installed, used, upgraded, rolled back, and recovered on its declared supported surfaces, with verifiable release assets and honest limitations. | Release packaging/publishing pipeline; Public credential-free build/fork lane and separate maintainer refresh/signing; Sigstore identity and offline bundles; macOS notarization and Windows signing; Homebrew/APT/WinGet channels; exact version pin/cache/atomic updates; minimal/no-account and second synthetic deployment plus dogfood lanes; full OS/browser/adapter matrix; security/failure integration; clean-node and operator acceptance. | Actual required platform lanes, end-to-end CLI/Console/agent workflows, supply-chain/tamper denials, install/upgrade/rollback, isolated disaster restore and writer fencing, all phase exits and required runbooks. Release publication and site activation need their own authorization; unavailable required real-platform proof keeps this phase open. |

Early phases test release-verification primitives and bootstrap with controlled artifacts in isolated, explicitly scoped test environments. Phase 11 proves the final distribution and complete supported matrix. A test artifact is not a published production release, and an earlier mock is not evidence for a later live gate. The user's chosen rollout timing excludes an early lab onboarding rehearsal.

## Delivery path from development to the lab

Use the phases above as the only development sequence. The checkpoints below connect that sequence to the intended operator outcome; they are not additional phases, issue IDs or a second backlog.

| Checkpoint | Work and ownership | Evidence needed to move on |
|---|---|---|
| **Plan the first batch — phase 0** | Approve the [phase 0 solution](phases/00-development-foundation.md), development authority, reproducible checks, bootstrap boundary and hardening contract. Convert only the next approved batch into complete GitHub issues. | Each issue has settled consequential choices, exact checks, available inputs, permitted review/merge and its estimate/timebox. Drafts are not `ready`. |
| **Build the foundations — phases 1–5** | Implement the one executable, contracts, database/API, Console, exact-plan engine, evidence, secret handling and recovery. Development uses local/hosted test infrastructure independent of the future lab runners. | Required contract, denial, failure, restore and interface-parity tests pass on their declared environments; unimplemented operations fail explicitly. |
| **Build the complete operating workflows — phases 6–10** | Implement generic local control-plane setup, account-free SSH inspection/preparation/recovery, Slack-only v1 human acknowledgement, OS/role hardening, identity/network adapters, hosting/registry, CI, monitoring and agent/manual workflows. Test against isolated targets and scoped provider test resources, without onboarding the inventory fleet. | Actual supported-OS tests, reboot/idempotence/lockout recovery, Slack workspace/user/replay/outage denial, adapter integration, cross-user/job denials and end-to-end bootstrap/handoff/rebuild proof. Mock proof cannot replace required real-platform proof. |
| **Accept the full software — phase 11** | Integrate every accepted capability, qualify all required support lanes, verify distribution/install/update/rollback and document limitations. Release publication retains its separate authorization. | All development exits and required tests pass; the deployment artifact is pinned and verifiable before use. Missing OS, provider, signing or recovery proof leaves the applicable acceptance open. |
| **Prepare the actual site — deployment phases 0–1** | Reconcile the physical inventory, installed OS/data, LAN/router/switch and recovery access. Start the restricted local control service on the selected host through the generic setup contract; use its scoped plans for guided reservations and approved host preparation. No separate operator workstation is required. | Correct identities, nonconflicting persistent addresses, qualified hardware/links, effective hardening and LAN administration without Cloudflare. Actual values and evidence stay private. |
| **Prove remote access — deployment phase 2** | Run the explicitly authorized Mesh/device pilot through the implemented workflow. Continue its existing tests for the actual 30 days. | Allowed/denied flows, reconnect/update/WAN-loss, revocation and LAN recovery meet the gate. This wait occurs after full software acceptance under the selected rollout timing. |
| **Establish central control — deployment phase 3** | Qualify the already locally initialized control host (`vsk-node-04` in this profile); do not repeat bootstrap or create a second authority. Configure the approved access, Coolify control, backup and monitoring components in their own plans. | Protected CLI/Console parity, one authoritative controller and a functional clean-spare restore. No user apps or CI on control. Entry evidence and post-install acceptance must be reconciled in phase 0 before this workflow is implemented. |
| **Configure roles and services — deployment phases 4–5** | Follow the canonical [inventory roles](../inventory-and-roles.md#deterministic-identity): CI on 01/06, applications on 03/05/07, Mac development/CI on 09, Hermes on 10; preserve 02 as qualified spare and 08 as reserve. Establish qualified ingress and Harbor placement. | Role capacity/isolation, effective firewalls after service installation, safe CI enrollment, signed image delivery, ingress denials/failover and registry recovery. Select the connector pair/marker from qualified application nodes before their activation; general application-by-application placement remains outside v1. |
| **Accept the lab — deployment phase 6** | Demonstrate normal operation, maintenance, onboarding/offboarding, alerts, break-glass and recovery through the same CLI/Console/agent/manual contracts. | Current site evidence and measured recovery objectives pass. Only now claim the complete lab setup accepted. |

Permitted read-only physical/network fact gathering may happen before software completion; it is preparation, not an early onboarding pilot, and time-sensitive evidence must be refreshed at rollout. No hardware, IP, provider or recovery fact is inferred from this roadmap. Every live stage still needs exact prerequisite evidence and authorization.

Keep development fast by detailing one batch at a time, reusing shared checks, testing each capability as it is built, and fixing routine review failures within the issue. Do not wait until phase 11 to test integration, introduce a second installer or maintain separate Codex/Claude playbooks. The full scope remains intact; no blanket promise of a completion date precedes executable issue scopes and measured test runtimes.

## Coverage map

This is a scope ownership map, not a second requirements specification. Phase planning must enumerate the relevant requirements and map them to issue acceptance criteria; the table alone is not proof that every requirement has been implemented.

| Canonical source | Development owners and coverage |
|---|---|
| [README and architecture boundaries](../../README.md) | All phases; phase 0 maintains scope/exclusions, phase 11 proves the complete product, and the separate deployment sequence below proves the lab profile. |
| [Portable lifecycle](../platform-lifecycle.md) | 0: generic/profile and setup contract; 1–5: metadata, local identity/credentials, effective authority and recovery; 6–7: control/operator/node enrollment; 8–10: workload and safe-exit integration; 11: minimal, non-Labs and Labs lifecycle acceptance. |
| [Automation and agents](../automation-and-agents.md) | 1: metadata/CLI/schemas/portable clients/release verifier; 2: server/state; 4: plan/apply/approval/adapter contract; 6: host automation; 9: CI; 10: focused skills/manual paths; 11: packages, support matrix and release acceptance. |
| [Control-plane service and Console](../control-plane-service.md) | 2: storage/API/service; 3: read Console and authentication; 4: declarations/execution UI; 5: evidence/recovery; 6: bootstrap; 7–10: corresponding adapters and complete screens; 11: integrated acceptance. This preserves the document's foundation → read UI → declarations → controlled execution → adapters → acceptance progression. |
| [Architecture and networking](../architecture-and-networking.md) | 6: base hosts, LAN and role boundaries; 7: Mesh, marker, Access/Tunnel and flows; 8: Coolify/Harbor; 9: CI; 10: operations; actual network/role qualification stays in deployment. |
| [Inventory and roles](../inventory-and-roles.md) | 2: identity/inventory model; 5: qualification evidence; 6: discovery, role placement, Macs, spare/reserve and replacement; 8: Harbor placement procedure; physical reconciliation remains deployment evidence. |
| [Security and operations](../security-and-operations.md) | 2–4: identity/session/authorization; 5: secrets, backup and recovery; 6–7: hosts/people/devices/access; 8–9: application/runner isolation; 10: monitoring, incidents and maintenance; 11: complete denial/recovery/support acceptance. |
| [Implementation gates](../implementation-gates.md) | 1/5 implement generated definitions, evidence and evaluation; each domain phase implements its collectors/checks; phase 11 verifies integration. All 23 gate IDs retain their original design/activation distinction, including conditional G-022 and deferred G-023. |
| [Decisions and sources](../decisions-and-sources.md) | Every phase traces its requirements to current decisions and distinguishes optional, superseded, excluded, and required work. Recheck version-sensitive provider facts before implementing the affected adapter/release; do not reopen settled choices without a demonstrated conflict. |
| [Repository agent contract](../../AGENTS.md), audit register and prior audit reports | 0: development authority/discovery and historical audit provenance; 10: generated agent instruction/skill behavior; 11: current acceptance evidence. Historical reports never substitute for tests of new code. |

## Existing live-deployment phases remain required

These are the deployment phases from the root README, **not additional development IDs**. The capability links identify software owners; they are not a replacement admission checklist. The [gate ledger](../implementation-gates.md#gate-ledger) determines exact live prerequisites, evidence and authority.

| Deployment phase | Required real-world outcome | Related development capability owners |
|---|---|---|
| **0 — Review and discovery** | Restricted local control setup and scoped inventory/recovery discovery; no blanket site qualification claim. | 2, 5, 6; read-only/manual discovery can occur earlier within existing permission. |
| **1 — LAN and base OS** | Qualified wired LAN and hardened foundation hosts, including selected application-node connectors/marker; reservations, denied listeners and Cloudflare-independent administration proven without admitting user workloads. | 1, 5, 6, with applicable release verification. |
| **2 — Identity and Mesh pilot** | Actual device/audience policy and 30-day Mesh pilot, permitted/denied paths and revocation/LAN recovery. | 5–7; real pilot time is an external wait, not agent effort. |
| **3 — Control plane** | Qualify the existing local service, including minimal protected Console ingress and its G-012 proof, Coolify, backup/monitoring and clean-spare recovery without user apps or CI on control. | Foundations 1–5, bootstrap 6, access 7, hosting 8, monitoring 10, applicable signed release/acceptance from 11. |
| **4 — CI and application capacity** | Four qualified Linux jobs, safe Mac lane/fallback, Hermes resource limits, trusted-event isolation and spare-role rehearsal. | 6, 8–11. |
| **5 — Ingress and Harbor** | Qualified redundant connectors, real registry migration/restore, allowed/denied access and signed digest deployment. | 5, 7–9 and applicable release/acceptance. |
| **6 — Operations acceptance** | Actual onboarding/offboarding, maintenance, alert, incident, backup and recovery exercises meet the documented targets. | All development phases plus every applicable live gate. |

No gate is marked passed by this roadmap. Development may use isolated fixtures without waiting for unrelated physical/provider evidence. Live work is admitted only when its own prerequisites exist and the user grants the required authority.

## Implementation-readiness

The current requirements include the confirmed OSS lifecycle, account-free read/preparation/recovery path, Slack-only v1 human acknowledgement and operational corrections. This is readiness for phase solution/issue preparation, not a blanket claim that every issue or test environment is ready. Requirements completeness, executable issue readiness, software acceptance and actual site acceptance are separate.

| Work | Remaining prerequisite and owner |
|---|---|
| Named-repository development / first delivery | 0.1: explicit development-only permission and an inspected satisfiable review/check/merge route; no repository administration, release or fleet authority implied. |
| Public reproducible build and checks | 0.2: exact pinned toolchain, approved redistribution/dependency provenance, credential-free public checks and protected maintainer lanes; preserve historical generated audit files. |
| Generic local setup and profile boundaries | 0.3 pins manifest/identity/transport fields, operation-specific entry versus post-install evidence, profile applicability and interruption states to the portable lifecycle. No workstation or Labs node number is a core prerequisite; missing Slack explicitly blocks normal bootstrap/apply. |
| Human acknowledgement boundary | 0.3 selects Slack-only v1 and contract fixtures; #7 implements the provider-neutral request/proof and Slack orchestration, #9 the Slack-user/local-principal mapping, #12 logical Slack secret delivery, and #15 review/approve/reject presentation. #14 notifications cannot authorize. Runtime and real-workspace proof remain later phase gates; account-free approval is deferred beyond v1. |
| Provider credential boundaries | Phase 5/G-007: review physical vault partitions by reader set and direct-provider denial tests. Phase 7/G-012: obtain approval for the shared tunnel credential/compromise interruption contract or review an alternative; no topology/cost expansion inferred. |
| Host security / native credentials / access | [0.4 (#17)](https://github.com/vegastack/vegastack-labs/issues/17) pins `host-security-v1`, the exact-plan one-shot privilege boundary, account-free per-consumer resolver, Mesh-only automated Mac SSH/local consent and daily/after-change admission transitions. Phase 5 owns credential/evidence engine implementation; Phase 6 owns real Debian/Ubuntu/macOS enforcement, reboot and idempotence; Phase 7 owns Mesh policy integration; Phase 10 owns drift/repair; Phase 11 proves the supported matrix. Selected outcomes cannot be silently weakened if a mechanism fails. |
| Backup mechanism | Phase 5: qualify mutable restic locks versus protected payloads and retained dependency lifetime; if incompatible, bring a concrete safe alternative for review before adapter implementation is ready. |
| CI assignment boundary | Phase 9: concrete actual-job admission mechanism before user code/secrets, tested under provider dispatch races; labels alone are not enforcement. |
| Private application endpoints | Phases 7–8: qualify normal application routing/TLS and supported client paths; no Harbor-specific extra privileges or implicit general DNS. |
| Device/site evidence | Actual hardware/firmware/addresses, reservation-pool compatibility, possible HTTP-only management risk, secrets/recovery ownership and test resources remain scoped inputs. They block only affected work, not unrelated foundational development. |
| Executable protocol detail | Phases 1–5: canonical serialization, idempotency, leases/session renewal, pagination/events and evidence freshness are pinned in the owning issue before readiness. Existing fixed values such as 30-minute plan validity remain unchanged. |

For each item, the owning phase proposes and tests the concrete mechanism; an unresolved material choice stays a planning/design issue rather than being handed to an execution agent to invent. Requirements may specify the required safety outcome before all site evidence exists. Missing mandatory proof is never waived or labeled complete by an audit.

## Inputs and approvals, at the right time

**Current approved batch:** Phase 0 issues [0.1 (#3)](https://github.com/vegastack/vegastack-labs/issues/3) through [0.6 (#22)](https://github.com/vegastack/vegastack-labs/issues/22) are completed and the phase exit was accepted on 07-09-2026. Phase 1 Issues [1.1 (#24)](https://github.com/vegastack/vegastack-labs/issues/24) and [1.2 (#25)](https://github.com/vegastack/vegastack-labs/issues/25) are merged with their integrated checks passing. Brief v2 and Plan v1 for [1.3 (#28)](https://github.com/vegastack/vegastack-labs/issues/28) are approved; it owns the remaining local release-verification slice and combined Phase 1 exit. Pull requests, merges, Phase 1 exit acceptance, release, repository administration, providers, credentials, hosts, networks, and live infrastructure retain separate gates. Roadmap approval and full-v1-before-rollout timing are already recorded; do not re-ask them.

**Before remote execution:** require an approved named-repository development batch and separate authorization for any proposed repository-policy change. Inspect current rules and use the agreed PR/check/review/merge route without bypass. Phase 0 batch 1 permits its issue/milestone, branch, commits, PR, development comments/reviews and merge after checks and fresh review; it permits no new credentials, settings/rulesets, release or infrastructure action.

**Later, only when needed:** obtain unavailable real OS/test lanes and signing identities, actual provider/secret/recovery ownership, site/physical measurements, alert recipients, Harbor migration inputs, and service mappings at their affected gates. Discover facts read-only where permitted before asking the user. Never put private evidence or secret values in GitHub.

Execution estimates belong to each detailed phase/batch once issue scope, dependencies, test runtime and available environments are known. Use the mandate's agent-minute basis and 2× checkpoint; do not invent precise estimates for this unimplemented full-platform roadmap.

## Path to the first completed issue

1. Completed on 27-08-2026: approve Phase 0 batch 1, publish its milestone and [issue 0.1](https://github.com/vegastack/vegastack-labs/issues/3), and claim the issue on `chore/0.1-development-route`.
2. Completed on 27-08-2026: run issue 0.1's deterministic checks, fresh review and in-scope fixes; merge through the approved route only after the evidence is current.
3. Completed on 27-08-2026: verify the integrated commit on `main`, post the implementation/evidence summary, and close issue 0.1.
4. Completed on 27-08-2026: use the pilot findings to prepare and obtain approval for [issue 0.2 (#5)](https://github.com/vegastack/vegastack-labs/issues/5) as Phase 0 batch 2.
5. Completed on 04-09-2026: deliver [issue 0.3 (#16)](https://github.com/vegastack/vegastack-labs/issues/16) and merge its bootstrap/profile/transport/Slack contract corpus.
6. Completed on 04-09-2026: deliver [issue 0.4 (#17)](https://github.com/vegastack/vegastack-labs/issues/17), including its public contract checks and fresh security-focused review.
7. Completed on 07-09-2026: deliver and merge [issue 0.5 (#18)](https://github.com/vegastack/vegastack-labs/issues/18) as the integrated Phase 0 evidence, consistency and handoff check; its integrated local check and PR #21 hosted check passed.
8. Completed on 07-09-2026: deliver and merge [issue 0.6 (#22)](https://github.com/vegastack/vegastack-labs/issues/22) through PR #23, verify its integrated public check, and record the operator's Phase 0 exit acceptance.
9. Completed on 07-09-2026: create and approve the bounded Phase 1 briefs [1.1 (#24)](https://github.com/vegastack/vegastack-labs/issues/24) and [1.2 (#25)](https://github.com/vegastack/vegastack-labs/issues/25), then approve Plan v1 for Issue 1.1 after settling incremental coverage, typed-Go ownership, and checked-in generation.
10. Completed on 07-09-2026: implement and merge Issue 1.1's metadata graph, generated artifacts, drift checks, documentation and verification through PR #26; its integrated public check passed.
11. Completed on 07-09-2026: implement Issue 1.2's generated-registry executable, portable client boundaries, target-build verifier, documentation, verification and fresh review.
12. Completed on 07-09-2026: merge Issue 1.2 and pass its integrated check.
13. Current: implement and review Issue 1.3, then merge only after a separate operator instruction, pass the integrated public lane, and present the combined Phase 1 exit demonstration for explicit operator acceptance. Phase 2 briefs are approved but remain parked until that acceptance.
